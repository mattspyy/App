import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // Next 16's react-hooks/set-state-in-effect rule flags legitimate
      // "load on mount / guarded auto-default" patterns used heavily by
      // this codebase. The flagged sites all have explicit guards that
      // prevent cascading renders, so silence the rule here rather than
      // refactor every page to introduce a query library.
      "react-hooks/set-state-in-effect": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
